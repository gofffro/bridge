# bridge
RemoteControl Ч абстракци€, управл€юща€ устройством.

IDevice Ч интерфейс дл€ разных устройств (реализаций).

WindowsDevice и TVDevice Ч конкретные реализации устройств.

¬ Main создаЄм разные устройства и управл€ем ими через один и тот же интерфейс RemoteControl.

ѕаттерн Bridge раздел€ет абстракцию и реализацию, чтобы их можно было измен€ть независимо.a